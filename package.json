{
  "name": "jordglobe",
  "version": "1.0.0",
  "private": true,
  "description": "JordGlobe monorepo - geography learning platform",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@firebase/util",
      "@parcel/watcher",
      "better-sqlite3",
      "esbuild",
      "protobufjs",
      "sharp"
    ]
  },
  "scripts": {
    "dev": "pnpm dev:site & pnpm dev:games",
    "dev:all": "pnpm dev:site & pnpm dev:games",
    "dev:games": "pnpm --filter @jordglobe/games dev",
    "dev:site": "pnpm --filter @jordglobe/site dev",
    "build:games": "pnpm --filter @jordglobe/games build",
    "build:site": "turbo run build --filter=@jordglobe/site",
    "build:eurovision-embedded": "GAMES_SEO_CONFIG_PATH=$PWD/shared/games-seo.json turbo run build:eurovision-embedded --filter=@jordglobe/games",
    "deploy:stage": "GAMES_SEO_CONFIG_PATH=$PWD/shared/games-seo.json turbo run deploy:stage --filter=@jordglobe/site",
    "deploy:prod": "GAMES_SEO_CONFIG_PATH=$PWD/shared/games-seo.json turbo run deploy:prod --filter=@jordglobe/site",
    "preview": "GAMES_SEO_CONFIG_PATH=$PWD/shared/games-seo.json turbo run preview --filter=@jordglobe/site"
  },
  "keywords": [
    "geography",
    "quiz",
    "learning",
    "globe"
  ],
  "author": "",
  "license": "PRIVATE",
  "devDependencies": {
    "turbo": "^2.9.6",
    "wrangler": "^4"
  }
}
