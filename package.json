{
  "name": "jordglobe",
  "version": "1.0.0",
  "private": true,
  "description": "JordGlobe monorepo - geography learning platform",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@firebase/util",
      "@parcel/watcher",
      "better-sqlite3",
      "esbuild",
      "protobufjs",
      "sharp"
    ]
  },
  "scripts": {
    "dev": "pnpm dev:site & pnpm dev:games",
    "dev:site": "pnpm --filter @jordglobe/site dev",
    "dev:games": "pnpm --filter @jordglobe/games dev",
    "dev:party": "pnpm --filter @jordglobe/party-server dev",
    "dev:logs": "pnpm --filter @jordglobe/games log-server",
    "build": "turbo run build",
    "build:site": "turbo run build --filter=@jordglobe/site",
    "build:games": "turbo run build --filter=@jordglobe/games",
    "build:functions": "npm --prefix packages/functions run build",
    "preview": "turbo run preview --filter=@jordglobe/site",
    "deploy:stage": "turbo run deploy:stage --filter=@jordglobe/site",
    "deploy:prod": "turbo run deploy:prod --filter=@jordglobe/site",
    "deploy:functions": "npm --prefix packages/functions run deploy",
    "deploy:rules": "npm --prefix packages/functions run deploy:rules",
    "run:functions": "npm --prefix packages/functions run run:once",
    "logs:functions": "npm --prefix packages/functions run logs"
  },
  "keywords": [
    "geography",
    "quiz",
    "learning",
    "globe"
  ],
  "author": "",
  "license": "PRIVATE",
  "devDependencies": {
    "turbo": "^2.9.6",
    "wrangler": "^4"
  }
}
