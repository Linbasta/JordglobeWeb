---
import { getDownloadUrl } from '@js/DownloadFromSite';

const userAgent = Astro.request.headers.get('user-agent') || '';
const redirectUrl = getDownloadUrl(userAgent, true);
return Astro.redirect(redirectUrl);
---
