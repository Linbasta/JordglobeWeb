/**
 * Party Application Index
 * Exports for the multiplayer party game (both host and client)
 */

// Note: Party application modules are typically imported directly from their folders
// since host and client are separate entry points with different HTML files.
// This index exists for consistency and potential future use.

// Currently, party modules are accessed via:
// - /src/party/host/lobby.ts (for host.html)
// - /src/party/client/main.ts (for party.html)
