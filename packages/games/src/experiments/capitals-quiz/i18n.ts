import type { QuizTranslations } from '../../shared/i18n/types';

export const CAPITALS_QUIZ_I18N: QuizTranslations = {
    defaultLocale: 'en',
    availableLocales: [{ code: 'en', label: 'English' }],
    strings: { en: {} },
};
