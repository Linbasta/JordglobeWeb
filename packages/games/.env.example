# Environment Configuration
# Copy this file to .env for local development

# Server Port (Cloud Run uses 8080)
PORT=8080

# Node Environment
NODE_ENV=production

# Optional: Set in Cloud Run for logging level, etc.
# LOG_LEVEL=info
